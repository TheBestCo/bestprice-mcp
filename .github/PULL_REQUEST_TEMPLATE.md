## What

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `npm run check` and `npm test` pass locally
- [ ] Every package manifest still carries the same version (`test/manifests.test.js` enforces this)
- [ ] Docs updated if install steps, tool lists, or environment variables changed
- [ ] `CHANGELOG.md` has an entry under **Unreleased** when the change is user-visible
